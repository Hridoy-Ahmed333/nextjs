import ShowForm from "@/Components/form/ShowForm";
//Press Ctrl + shift + r to refresh
const Create = () => {
  return (
    <div>
      <ShowForm content="anime" />
    </div>
  );
};

export default Create;
