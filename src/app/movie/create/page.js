import MovieForm from "@/Components/form/MovieForm";

const Create = () => {
  return (
    <div>
      <MovieForm content="movie" />
    </div>
  );
};

export default Create;
//Press Ctrl + shift + r to refresh
