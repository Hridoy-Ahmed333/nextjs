import SeriesForm from "@/Components/form/SeriesForm";

const Create = () => {
  return (
    <div>
      <SeriesForm content="series" />
    </div>
  );
};

export default Create;
//Press Ctrl + shift + r to refresh
